POCKET AI 16.0 HYBRID ARCHITECTURE

Normal ladder: 0.6B -> 0.8B -> 1.5B -> 1.7B -> 2B -> 3.09B.
Emergency fallback: 135M Q2_K / Q4_1. Optional high-memory tiers: 4B / 8B.

Adjacent hybrid route:
1. Smaller model receives compressed evidence and creates a plan/draft.
2. The process exits and memory is released.
3. RAM and temperature are checked again.
4. The next stronger model verifies and synthesizes the answer.
5. Deterministic tools and evidence audits can repair the result.

Only one GGUF is loaded at a time. Hybrid parameters are never added.
